#include <stdio.h>

class Dog {
    pub String name;

    pub Dog(String name) {
        this.name = name;
    }

    pub bark() {
        printf("Woof!\n");
    }
}

main() -> int {
    Dog dog = new Dog("Rex");
    dog.bark();
}
